{
  "name": "myucto-mcp",
  "version": "1.0.0",
  "description": "MCP server pro MyÚčto.cz: fakturace, mzdy, zakázky, dokumenty, kniha jízd, e-shop/sklad a statistika",
  "license": "MIT",
  "type": "module",
  "main": "src/index.mjs",
  "bin": {
    "myucto-mcp": "src/index.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "start": "node src/index.mjs",
    "build": "node build.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.mjs && node --check src/client.mjs && node --check src/tools.mjs && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
