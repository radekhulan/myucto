<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         displayDetailsOnTestsThatTriggerWarnings="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
        <testsuite name="Architecture">
            <directory>tests/Architecture</directory>
        </testsuite>
        <!-- Invarianty (L3): tvrzení, která platí nezávisle na datech. Neseedují nic,
             měří databázi tak, jak je — chytí i to, co do dat dostala cesta, na kterou
             žádný scénářový test nedosáhne (backfill, import, ruční zásah). -->
        <testsuite name="Invariants">
            <directory>tests/Invariants</directory>
        </testsuite>
    </testsuites>
    <!-- Sdílené DB spojení testovacího běhu potřebuje po každém testu úklid session
         stavu + hlasité nahlášení rozdělané transakce. Viz SharedTestConnectionGuard. -->
    <extensions>
        <bootstrap class="MyInvoice\Tests\Support\SharedTestConnectionGuard"/>
    </extensions>
    <source>
        <include>
            <directory suffix=".php">src</directory>
        </include>
    </source>
    <php>
        <!-- Sada roste; při 512M padal plný běh fatálem v PayrollEnumContractTest. -->
        <ini name="memory_limit" value="1G"/>
    </php>
</phpunit>
